# ==========================================
# 环球观察速递
# AIModelClient V1.0
#
# 统一AI模型调用接口
#
# 当前版本：
# 先建立统一接口
# 暂不连接具体AI API
# ==========================================


class AIModelClient:

    # ==========================================
    # 初始化
    # ==========================================

    def __init__(self):

        self.name = "AI模型客户端"

    # ==========================================
    # AI分析接口
    # ==========================================

    def analyze(self, prompt):

        prompt = str(
            prompt
        ).strip()

        if not prompt:

            raise ValueError(
                "AI分析内容不能为空"
            )

        # ======================================
        # 当前V1.0暂时使用模拟返回
        #
        # 后续这里接入：
        # OpenAI / Claude / Gemini / 本地模型
        # ======================================

        return {

            "status": "MOCK",

            "message":
                "AI模型接口测试成功",

            "prompt":
                prompt

        }